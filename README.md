# sbb_ner_hf
sbb ner pretraining and finetuning with huggingface

## installation
Install python --version 3.10.14 (e.g. in pyenv virtualenv) plus the libraries from [requirements.txt](requirements.txt) file.